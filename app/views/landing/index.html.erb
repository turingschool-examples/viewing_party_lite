
<h1> Viewing Party Lite </h1>

<%= button_to "Create new user", register_path, method: :get %>

<% @users.each do |user| %>
  <p><%= link_to "#{user.email}'s Dashboard", user_path(user)%></p>
<% end %>
