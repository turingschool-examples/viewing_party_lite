<center>
<h1>Viewing Party Lite</h1>

<h3>Login or Register</h3>
<%= button_to "Register as a User", new_user_path, method: :get %><br>
<%= button_to "Log In", "/login", method: :get %>

<h3>Current Users</h3>
<div id="users">
  <% @users.each do |u| %>
    <p><%= link_to "#{u.name}", user_path(u) %></p>
  <% end %>
</div>

</center>