<h1>Viewing Party Lite</h1>

<%= button_to 'New User', '/register', method: :get  %>
<% @users.each do |user| %>
<h3><%= link_to "#{user.name}", "/users/#{user.id}" %>: <%= user.email %></h3>
<% end %>
