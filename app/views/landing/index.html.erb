<h1> Viewing Party Lite </h1>

<%= button_to "Create a New User", "/register", method: :get %><br>

<div id="existing_users"> Existing Users:<br>
  <% @users.each do |user| %>
    <%= link_to "#{user.email}'s Dashboard", "/users/#{user.id}" %><br>
  <% end %>
</div>