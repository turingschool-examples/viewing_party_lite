<center>
<h1>Viewing Party Lite</h1>

<%= button_to "Create New User", new_register_path, method: 'get' %>

<% @users.each do |u| %>
  <p><%= link_to "#{u.name}", user_path(u) %></p>
<% end %>
</center>