
<%= button_to "Discover Top Rated Movies", user_movies_path(@user.id), method: :get %>

<h4>Search Movies</h4>
<%= form_with url: user_movies_path(@user.id), method: :get, local: true do |f| %>
  <%= f.label :keyword %>
  <%= f.text_field :keyword %>
  <%= f.submit 'Search' %>
<% end %>