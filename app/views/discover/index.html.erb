<p><%= button_to "Top Rated Movies", user_movies_path(@user), method: :get %></p>

<h1>OR</h1>

<%= form_with url: user_movies_path(@user), method: :get, local: true do |form| %>
    <p><%= form.label :search, "Search by movie title"%></p>
    <%= form.text_field :search, required: true %>

    <p><%= form.submit "Find Movies" %></p>
<% end %>
