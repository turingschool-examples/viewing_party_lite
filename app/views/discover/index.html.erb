<%= button_to "Top Rated Movies", user_movies_path(@user), method: :get %>

<%= form_with url: user_movies_path(@user), method: :get, local: true do |f| %>
  <%= f.label :movie_search %>
  <%= f.text_field :movie_search %>
  <%= f.submit "Search" %>
<% end %>