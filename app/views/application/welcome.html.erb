<h1>Viewing Party Lite</h1>
<%= button_to "Create New User", "/users/new" %>
<% @users.each do |user| %>
  <div id=user-<%="#{user.id}"%>>
    <%= link_to user.name, "/users/#{user.id}" %>
  </div>
<% end %>