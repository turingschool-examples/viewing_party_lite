<h1>Viewing Party</h1>
<%= button_to "Create New User", register_path, method: :get %>
<p>
<div id="user-list">
  <h3>Current Users</h3>
  <% @users.each do |user| %>
    <div id="user-<%="#{user.id}"%>">
      <%= link_to user.name, user_path(user) %><br>
    </div>
  <% end %>
</div>