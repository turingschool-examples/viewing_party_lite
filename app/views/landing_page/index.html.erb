<h1> Viewing Party </h1>
<%= button_to "Create User", "/register", method: :get %>

<h3>Users:</h3>
<hr>
<p><% @users.each do |user| %></p>
  <p><%= link_to "#{user.name}", "/users/#{user.id}" %></p>
<% end %>
