<center><%= button_to 'Create a New User', register_path, method: :get %>

<div id="existing-users">
  <h2>Existing Users</h2>
  <% @users.each do |user| %>
    <p><%= link_to "#{user.email}", user_path(user) %></p>
  <% end %>
</div></center>