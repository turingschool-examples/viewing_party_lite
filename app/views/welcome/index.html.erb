<section id="title">
  <h1>Welcome to Viewing Party</h1>
</section>

<section id="new-user">
  <p><%= button_to 'Register', '/register', method: :get %><p>
</section>

<section id="users">
   <% if logged_in? %> 
    <% @users.each do |user| %>
        <p><%= user.email %><p>
    <% end %>
  <% end %>
</section>