<section id="title">
  <h1>Welcome to Viewing Party</h1>
</section>

<section id="new-user">
  <p><%= button_to 'Register', '/register', method: :get %><p>
</section>

<section id="users">
  <% @users.each do |user| %>
      <p>Name: <%= link_to "#{user.name}", user_path(user.id) %><p>
  <% end %>
</section>