
<%= button_to 'Create a New User', register_path, method: :get %>

<h3> Existing Users: </h3>

<% @users.each do |user| %>

  <p><%= link_to "#{user.email}'s Dashboard", "/users/#{user.id}", method: :get %></p>
<% end %>
