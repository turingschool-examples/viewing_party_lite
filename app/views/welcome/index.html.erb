<%# <h1> Viewing Party Lite's Movies! </h1>
<% @top_movies.each do |movie| %>
<p><%# movie.title %> <%# movie.vote_average %> </p>
<%# end %> 

<%= button_to("Create a New User", "/users/new")  %>

<div id='existing-users'>
    <p> Existing Users </p>
    <% @users.each do |user| %>
        <%= link_to("#{user.email}'s Dashboard", "/users/#{user.id}") %>
    <% end %>
</div>