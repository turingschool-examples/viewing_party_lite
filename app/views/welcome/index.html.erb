

<h1>Viewing Party Lite</h1>
<%= link_to "Login", "/login" %>
<%= button_to "Create a New User", "/register", method: :get %>

<h3> Current Users </h3>

<% @users.each do |user| %>
  <div id="user-<%= user.id %>">
    <p><%= link_to "#{user.email}", user_path(user) %>'s Dashboard</p>
  </div>
<% end %>
