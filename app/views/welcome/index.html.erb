<%= render partial: "shared/header" %>

<h1>
  Welcome to Viewing Party Lite
</h1>

<%= button_to 'New User', new_user_path, method: :get %>
<%# @users.each do  |user| %>
  <h4>
    <%#= link_to "#{user.name}", user_path %>
    </h4>
    <%# end %>