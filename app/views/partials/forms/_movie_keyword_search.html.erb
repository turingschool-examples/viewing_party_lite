    <%= form_with url: "/users/#{params[:id]}/movies", method: :get, local: true do |form| %>
    <%= form.text_field :search, placeholder: "Search by Movie Title" %>
    <%= form.submit "Find Movies" %>
    <% end %>