<center>
<%= button_to "Discover Top Movies", user_movies_path(@user), method: :get %> 
<%= form_with url: user_movies_path(@user), method: :get, local: true do |form| %> 
  <%= form.label :title_search, 'Title Search' %>
  <%= form.text_field :title_search %>
  <%= form.submit 'Search' %>
<% end %>
</center>
