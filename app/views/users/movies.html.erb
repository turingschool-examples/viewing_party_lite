<h1>Your Movie Results Are Ready!</h1>

<%= button_to "Back to Discover", "/users/#{@user.id}/discover", method: :get %>

<% @movies[0..39].each_with_index do |movie, index| %>
<h3><%= movie.title %></h3>
  <p>Rating: <%= movie.vote_average %></p>
  <br>
<% end %>
