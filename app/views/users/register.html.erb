<h1> New User Registration </h1>

<section id= "registration_form">

<%= form_with model: @user, url: "users/id", local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.label :email%>
  <%= f.text_field :email %>
  
  <%= f.submit "Register" %>
<% end %>

</section>