<p><%= button_to "Register User", register_path, method: :get %></p>

<h3>Existing Users</h3>
<% @users.each do |user| %>
  <p><div id="user-<%= user.id %>"><%= link_to "#{user.name}", user_path(user) %></div></p>
<% end %>