<h2>Users</h2>

<% @users.each do |user| %>
<div id="user-<%= user.id %>">
  <p><%= link_to "#{user.name}", user_path(user) %></p>
</div>
<% end %>

<%= button_to 'New User', register_path, method: :get %>
