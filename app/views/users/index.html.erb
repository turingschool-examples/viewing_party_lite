<h1>Viewing Party Lite</h1>

<%= link_to 'Create New User', new_user_path %>

<h3>Existing Users</h3>
<% @users.each do |user| %>
    <p><%= link_to "#{user.name} | #{user.email}", user_path(user) %></p>
<% end %>