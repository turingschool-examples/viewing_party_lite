<center>
  <h2> Register a New User</h2>

  <%= form_with url: users_path, method: :post, local: true do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>
    <%= f.label :email %>
    <%= f.text_field :email %><br>
    <br><%= f.submit "Register" %><br>
  <% end %>
</center>
