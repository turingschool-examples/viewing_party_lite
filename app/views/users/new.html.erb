<h1>Sign Up for your FREE Viewing Party PRO Account TODAY!</h1>

<%= form_with url: "/users", method: :post, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit "Register" %>
<% end %>
