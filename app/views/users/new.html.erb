<h2>Create New User</h2>

<%= form_with url: '/register', method: :post, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit :Submit%>
<% end %>
