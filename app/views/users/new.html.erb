<h1>Register a New User</h1>

<%= form_with model: @user, local: true do |form| %>
  <p><%= form.label :user_name, "Name:"%></p>
    <%= form.text_field :user_name%>
   <p><%= form.label :email, "Email:"%></p>
    <%= form.text_field :email%>

    <p><%= form.submit "Create User" %></p>
<% end %>