<!DOCTYPE html>
<html>
  <head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
    }

    .topnav {
      position: relative;
      overflow: hidden;
      background-color: #4B0082;
    }

    .topnav a {
      float: left;
      color: #f2f2f2;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 17px;
    }

    .topnav a:hover {
      background-color: #ddd;
      color: black;
    }

    .topnav a.active {
      background-color: #00BFFF;
      color: white;
    }

    .topnav-centered a {
      float: none;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .topnav-right {
      float: right;
    }

      @media screen and (max-width: 600px) {
        .topnav a, .topnav-right {
          float: none;
          display: block;
        }
        
        .topnav-centered a {
          position: relative;
          top: 0;
          left: 0;
          transform: none;
        }
      }
    </style>
  </head>

  <body>
    
    <div class="topnav"> <!-- Top navigation -->

      <div class="topnav-centered"> <!-- Centered link -->
        <a href="#ViewingPartyLight" class="active">Viewing Party Light</a>
      </div>

      <%= link_to 'Home', root_path %> <!-- Left-aligned links (default) -->

      <div class="topnav-right"> <!-- Right-aligned links -->
        <%= link_to "About", about_path %>
        <%# <a href="#about">About</a> %>
      </div>

    </div>
    
    <%= yield %>

  </body>


</html>
