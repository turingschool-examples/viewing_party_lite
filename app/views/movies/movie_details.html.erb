<%= button_to 'Create Viewing Party', "/users/#{@user.id}/movies/#{@movie_id}/viewing-party/new", method: :post, local: true %>

<%= button_to 'Discover Page', "/users/#{@user.id}/discover", method: :get, local: true %>
