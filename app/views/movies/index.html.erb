
<%= button_to "Discover Page", "/users/#{@user.id}/discover", method: :get%>

<br>

<div>
  <% @movies.each do |movie| %>
    <%= link_to "#{movie.title}", "/users/#{@user.id}/movies/#{movie.id}", method: :get %> <h3> Vote Average: <%= movie.vote_average %></h3>
  <% end %>
</div>

