<%=link_to 'Discover more movies', "/users/#{@user.id}/discover"%>

<% if params[:top] == "top_rated" %>
  <h1>Top Rated Movies</h1>
<% else  %>
  <h1>Keyword Movies</h1>
<% end %>

<% @movies.each do |m| %>
<div id="<%=m[:title]%>">
  <p>Title: <%=link_to "#{m[:title]}", user_movie_path(@user.id, m[:id]) %></p>
  <p>Vote Average: <%= m[:vote_average] %></p>
</div>
<% end %>
