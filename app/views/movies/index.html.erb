<div id="movies">
  <ol>
    <% @movies.each do |movie| %>
      <li><%= link_to movie.movie_title, "/users/#{@user.id}/movies/#{movie.id}" %> <%= "Vote Average: #{movie.vote_average}" %></li>
    <% end %>
  </ol>
</div>