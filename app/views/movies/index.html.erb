<%= link_to 'Discover Page', user_discover_path(@user.id) %>
<div class="topMovies">
<% @movies.each do |movie| %>
  <p class='movies'><%= link_to movie.title, user_movie_path(@user.id, movie.id) %>
  Average Vote: <%= movie.vote_average %></p>
<% end %>
</div>